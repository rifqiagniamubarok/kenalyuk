---
phase: 01-foundation-approval-system
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [
  "src/app/(auth)/register/page.tsx",
  "src/app/(auth)/login/page.tsx",
  "src/app/(auth)/verify-email/page.tsx",
  "src/app/api/auth/register/route.ts",
  "src/app/api/auth/verify-email/route.ts",
  "src/components/AuthForm.tsx",
  "src/lib/email.ts",
  "src/lib/password.ts"
]
autonomous: true
user_setup:
  - service: "email"
    why: "Email verification for user registration"
    env_vars:
      - name: "SMTP_FROM"
        source: "Gmail, Resend, or other SMTP provider"
      - name: "SMTP_HOST"
        source: "Email provider SMTP configuration"
      - name: "SMTP_USER"
        source: "Email provider SMTP credentials"
      - name: "SMTP_PASSWORD"
        source: "Email provider SMTP credentials"

must_haves:
  truths:
    - "User can register with email/password and receive verification email"
    - "User can verify email address before account activation"
    - "User can log in and maintain session across browser restarts"
    - "User can log out from any page"
    - "Registration validation prevents duplicate emails"
  artifacts:
    - path: "src/app/(auth)/register/page.tsx"
      provides: "User registration page"
      min_lines: 30
    - path: "src/app/api/auth/register/route.ts"
      provides: "Registration API endpoint"
      exports: ["POST"]
    - path: "src/components/AuthForm.tsx"
      provides: "Reusable authentication forms"
      contains: "register|login"
  key_links:
    - from: "src/app/(auth)/register/page.tsx"
      to: "src/app/api/auth/register/route.ts"
      via: "Form submission"
      pattern: "fetch.*api/auth/register"
    - from: "src/lib/email.ts"
      to: "src/app/api/auth/verify-email/route.ts"
      via: "Email verification process"
      pattern: "sendVerificationEmail"
---

<objective>
Implement complete user registration and authentication flow

Purpose: Enable users to register, verify email, and log in with secure session management
Output: Working registration system with email verification and persistent login sessions
</objective>

<execution_context>
../../../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Create user registration with email verification</name>
  <files>src/app/(auth)/register/page.tsx, src/app/api/auth/register/route.ts, src/lib/email.ts, src/lib/password.ts</files>
  <action>Build complete user registration system:
  - Create registration page with form (email, password, confirm password fields)
  - Implement API route for user registration with validation
  - Hash passwords using bcryptjs for security
  - Generate email verification tokens and store in database
  - Send verification email with secure token link
  - Validate email format, password strength, and prevent duplicate registrations
  - Set user status to PENDING_VERIFICATION until email confirmed
  Follow security best practices for password hashing and token generation</action>
  <verify>Registration form submits successfully, verification email sends, user created in database with PENDING_VERIFICATION status</verify>
  <done>User can register and receive email verification link, registration data validates correctly</done>
</task>

<task type="auto">
  <name>Implement email verification flow</name>
  <files>src/app/(auth)/verify-email/page.tsx, src/app/api/auth/verify-email/route.ts</files>
  <action>Create email verification process:
  - Build verification page that accepts token from email link
  - Implement API route to validate verification tokens
  - Update user status from PENDING_VERIFICATION to PENDING_APPROVAL after successful verification
  - Handle expired tokens with appropriate error messages
  - Provide resend verification email functionality
  - Auto-redirect verified users to biodata completion or dashboard
  Token should expire after 24 hours for security</action>
  <verify>Email verification link works correctly, user status updates to PENDING_APPROVAL, expired tokens handled properly</verify>
  <done>Email verification complete, user progresses from PENDING_VERIFICATION to PENDING_APPROVAL status</done>
</task>

<task type="auto">
  <name>Build login system with Auth.js integration</name>
  <files>src/app/(auth)/login/page.tsx, src/components/AuthForm.tsx</files>
  <action>Create secure login system:
  - Build login page with email/password form
  - Integrate with Auth.js credentials provider configuration
  - Create reusable AuthForm component for consistent styling
  - Implement client-side validation and error handling
  - Add logout functionality available from any page
  - Handle login redirects based on user role (user vs supervisor vs superadmin)
  - Ensure session persistence across browser restarts
  Use Auth.js signIn/signOut functions with proper error handling</action>
  <verify>Login form works with registered users, session persists across browser restart, logout clears session properly</verify>
  <done>Working login system with Auth.js integration, session management, and logout functionality</done>
</task>

</tasks>

<verification>
- Registration form creates users and sends verification emails
- Email verification process updates user status correctly
- Login system works with Auth.js and maintains sessions
- Logout functionality clears sessions from all pages
- Password security follows best practices with proper hashing
</verification>

<success_criteria>
- Complete AUTH-01: User registration with email/password using Auth.js
- Complete AUTH-02: User email verification before account activation
- Complete AUTH-03: User login with session persistence across browser restarts  
- Complete AUTH-07: User logout functionality from any page
- Registration and login flow ready for biodata completion phase
</success_criteria>

<output>
After completion, create `.gsd/phases/01-foundation-approval-system/01-03-SUMMARY.md`
</output>