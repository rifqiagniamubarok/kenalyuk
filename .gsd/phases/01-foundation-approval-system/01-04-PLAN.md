---
phase: 01-foundation-approval-system
plan: 04
type: execute
wave: 2
depends_on: ['01-01', '01-02']
files_modified:
  [
    'src/app/(superadmin)/superadmin/regions/page.tsx',
    'src/app/(superadmin)/superadmin/supervisors/page.tsx',
    'src/app/(superadmin)/superadmin/dashboard/page.tsx',
    'src/app/api/regions/route.ts',
    'src/app/api/supervisors/route.ts',
    'src/components/RegionManager.tsx',
    'src/components/SupervisorManager.tsx',
    'src/lib/audit.ts',
  ]
autonomous: true

must_haves:
  truths:
    - 'Superadmin can create and manage regions with name and description'
    - 'Superadmin can assign supervisor role to users for specific regions'
    - 'Superadmin can revoke supervisor permissions'
    - 'System maintains audit log of all supervisor actions'
    - 'Superadmin dashboard shows system overview and user statistics'
    - 'Role-based access control prevents unauthorized API access'
  artifacts:
    - path: 'src/app/(superadmin)/superadmin/regions/page.tsx'
      provides: 'Region management interface'
      min_lines: 50
    - path: 'src/app/api/regions/route.ts'
      provides: 'Region CRUD operations'
      exports: ['GET', 'POST', 'PUT', 'DELETE']
    - path: 'src/lib/audit.ts'
      provides: 'Audit logging functionality'
      exports: ['createAuditLog', 'getAuditHistory']
  key_links:
    - from: 'src/app/(superadmin)/superadmin/regions/page.tsx'
      to: 'src/app/api/regions/route.ts'
      via: 'Region management API calls'
      pattern: 'fetch.*api/regions'
    - from: 'src/components/SupervisorManager.tsx'
      to: 'src/lib/audit.ts'
      via: 'Action logging'
      pattern: 'createAuditLog'
---

<objective>
Implement complete role management and administration system

Purpose: Enable superadmin users to manage regions, assign supervisor roles, and monitor system activity
Output: Full superadmin dashboard with region management, supervisor assignment, and audit logging
</objective>

<execution_context>
../../../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Create region management system</name>
  <files>src/app/(superadmin)/superadmin/regions/page.tsx, src/app/api/regions/route.ts, src/components/RegionManager.tsx</files>
  <action>Build comprehensive region management:
  - Create superadmin regions page with CRUD interface
  - Implement API routes for region operations (GET, POST, PUT, DELETE)
  - Build RegionManager component with create, edit, delete region functionality
  - Add form validation for region name (unique, 3-50 chars) and description
  - Show region list with assigned supervisor count
  - Add confirmation dialogs for destructive actions
  - Ensure only SUPERADMIN role can access these routes
  Use optimistic UI updates for better user experience</action>
  <verify>Region CRUD operations work correctly, validation prevents invalid data, access restricted to superadmin role</verify>
  <done>Complete region management system with create, read, update, delete functionality</done>
</task>

<task type="auto">
  <name>Build supervisor assignment system</name>
  <files>src/app/(superadmin)/superadmin/supervisors/page.tsx, src/app/api/supervisors/route.ts, src/components/SupervisorManager.tsx</files>
  <action>Implement supervisor role management:
  - Create supervisor management page for role assignment
  - Build API routes to assign/revoke supervisor roles
  - Create SupervisorManager component with user search and region assignment
  - Show currently assigned supervisors by region
  - Allow multiple supervisors per region but prevent duplicate assignments
  - Add role change confirmation and immediate session update
  - Send notification to users when supervisor role assigned/revoked
  Update user role in database and invalidate existing sessions when role changes</action>
  <verify>Supervisor assignment works correctly, role changes update immediately, access control enforced properly</verify>
  <done>Working supervisor assignment and revocation system with proper role management</done>
</task>

<task type="auto">
  <name>Create audit logging and superadmin dashboard</name>
  <files>src/app/(superadmin)/superadmin/dashboard/page.tsx, src/lib/audit.ts</files>
  <action>Build audit system and overview dashboard:
  - Create audit logging utility for tracking supervisor actions
  - Implement dashboard showing system statistics (total users, active supervisors, pending approvals by region)
  - Add recent activity feed with audit log entries
  - Show user registration trends and approval rates by region
  - Create charts for user status distribution and regional activity
  - Log all supervisor actions (approvals, rejections, role assignments) with timestamp, user, and details
  Store audit logs in database table with user, action, timestamp, and metadata fields</action>
  <verify>Audit logs capture supervisor actions correctly, dashboard displays accurate statistics, charts render properly</verify>
  <done>Complete audit logging system with superadmin dashboard showing system overview and activity</done>
</task>

</tasks>

<verification>
- Region management allows full CRUD operations with proper validation
- Supervisor assignment system works with role-based access control
- Audit logging captures all administrative actions accurately
- Superadmin dashboard displays comprehensive system overview
- All role management features restricted to SUPERADMIN users only
</verification>

<success_criteria>

- Complete ROLE-01: Superadmin can create and manage regions
- Complete ROLE-02: Superadmin can assign supervisor role to users for specific regions
- Complete ROLE-03: Superadmin can revoke supervisor permissions
- Complete ROLE-04: System maintains audit log of all supervisor actions
- Complete ROLE-05: Role-based access control prevents unauthorized API access
- Complete ROLE-06: Superadmin dashboard shows system overview and user statistics
  </success_criteria>

<output>
After completion, create `.gsd/phases/01-foundation-approval-system/01-04-SUMMARY.md`
</output>
