---
phase: 01-foundation-approval-system
plan: 06
type: execute
wave: 3
depends_on: ["01-04"]
files_modified: [
  "src/app/(supervisor)/supervisor/dashboard/page.tsx",
  "src/app/(supervisor)/supervisor/pending/page.tsx",
  "src/app/(supervisor)/supervisor/history/page.tsx",
  "src/app/api/supervisor/pending/route.ts",
  "src/app/api/supervisor/approve/route.ts",
  "src/app/api/supervisor/reject/route.ts",
  "src/components/PendingUserCard.tsx",
  "src/components/ApprovalModal.tsx",
  "src/lib/notifications.ts"
]
autonomous: true
user_setup:
  - service: "notifications"
    why: "User notification system for approval status changes"
    env_vars:
      - name: "NOTIFICATION_METHOD"
        source: "Email, SMS, or in-app notification configuration"

must_haves:
  truths:
    - "Supervisor can view list of pending users in their assigned region"
    - "Supervisor can approve user profiles to change status to ACTIVE"
    - "Supervisor can reject user profiles with required reason text"
    - "User receives notification of approval/rejection status change"
    - "Re-approval workflow triggers when user edits biodata or photos"
    - "Supervisor can view approval history and previous decisions"
  artifacts:
    - path: "src/app/(supervisor)/supervisor/pending/page.tsx"
      provides: "Pending user review interface"
      min_lines: 50
    - path: "src/components/PendingUserCard.tsx"
      provides: "User profile review card"
      contains: "approve|reject"
    - path: "src/app/api/supervisor/approve/route.ts"
      provides: "User approval API endpoint"
      exports: ["POST"]
  key_links:
    - from: "src/components/PendingUserCard.tsx"
      to: "src/app/api/supervisor/approve/route.ts"
      via: "Approval action"
      pattern: "fetch.*api/supervisor/approve"
    - from: "src/app/api/supervisor/reject/route.ts"
      to: "src/lib/notifications.ts"
      via: "Rejection notification"
      pattern: "sendNotification"
---

<objective>
Implemente complete supervision and approval workflow system

Purpose: Enable supervisors to review and approve/reject user profiles with proper notifications and audit trails
Output: Working supervisor dashboard with approval workflows and user notification system
</objective>

<execution_context>
../../../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Create supervisor dashboard and pending user interface</name>
  <files>src/app/(supervisor)/supervisor/dashboard/page.tsx, src/app/(supervisor)/supervisor/pending/page.tsx, src/app/api/supervisor/pending/route.ts, src/components/PendingUserCard.tsx</files>
  <action>Build supervisor review interface:
  - Create supervisor dashboard with pending users count and regional statistics
  - Build pending users page showing PENDING_APPROVAL users in supervisor's assigned region
  - Create PendingUserCard component displaying user biodata, photos, and approval actions
  - Implement API route to fetch pending users filtered by supervisor's region
  - Add photo gallery view with navigation between user images
  - Include biodata review with all profile information displayed clearly
  - Ensure supervisors only see users from their assigned region
  Use grid layout for efficient review of multiple pending profiles</action>
  <verify>Supervisor dashboard loads correctly, pending users filtered by region, user profiles display completely</verify>
  <done>Working supervisor interface with pending user review and regional filtering</done>
</task>

<task type="auto">
  <name>Implement approval and rejection workflow</name>
  <files>src/app/api/supervisor/approve/route.ts, src/app/api/supervisor/reject/route.ts, src/components/ApprovalModal.tsx</files>
  <action>Build approval and rejection system:
  - Create approval API route that updates user status to ACTIVE
  - Create rejection API route requiring reason text (minimum 20 characters)
  - Build ApprovalModal component for confirmation and rejection reason input
  - Update user status in database and record supervisor action with timestamp
  - Log approval/rejection decisions in audit system
  - Prevent supervisors from acting on users outside their region
  - Add optimistic UI updates for immediate feedback
  Ensure proper validation and error handling for all approval actions</action>
  <verify>Approval changes user status to ACTIVE, rejection requires reason text, audit log captures actions</verify>
  <done>Complete approval workflow with proper validation, audit logging, and status updates</done>
</task>

<task type="auto">
  <name>Build notification system and approval history</name>
  <files>src/lib/notifications.ts, src/app/(supervisor)/supervisor/history/page.tsx</files>
  <action>Implement user notifications and supervisor history:
  - Create notification system to inform users of approval/rejection status changes
  - Send notification when user status changes to ACTIVE or REJECTED
  - Include rejection reason in rejection notifications
  - Build approval history page showing supervisor's past decisions
  - Display approval/rejection timeline with user info and reasons
  - Implement re-approval workflow detection when user edits biodata or photos
  - Reset user status to PENDING_APPROVAL when profile is modified after approval
  Use email notifications for immediate delivery, consider in-app notifications for future</action>
  <verify>Users receive notifications for status changes, approval history displays correctly, re-approval triggers on profile edits</verify>
  <done>Complete notification system with approval history and re-approval workflow on profile changes</done>
</task>

</tasks>

<verification>
- Supervisor dashboard shows pending users from assigned region only
- Approval and rejection workflows update user status correctly
- Notifications send to users when approval status changes
- Approval history tracking works and displays supervisor decisions
- Re-approval workflow triggers when users edit approved profiles
</verification>

<success_criteria>
- Complete SUP-01: Supervisor can view list of pending users in their assigned region
- Complete SUP-02: Supervisor can approve user profiles to change status to ACTIVE
- Complete SUP-03: Supervisor can reject user profiles with required reason text
- Complete SUP-04: User receives notification of approval/rejection status change
- Complete SUP-05: Re-approval workflow triggers when user edits biodata or photos
- Complete SUP-06: Supervisor can view approval history and previous decisions
</success_criteria>

<output>
After completion, create `.gsd/phases/01-foundation-approval-system/01-06-SUMMARY.md`
</output>