---
phase: 01-foundation-approval-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/lib/auth.ts",
  "src/middleware.ts",
  "prisma/schema.prisma",
  "src/types/auth.ts",
  "src/lib/validations.ts"
]
autonomous: true
user_setup:
  - service: "auth.js"
    why: "Authentication provider configuration"
    env_vars:
      - name: "NEXTAUTH_SECRET"
        source: "Generate random string for production security"
      - name: "NEXTAUTH_URL"
        source: "Application URL (http://localhost:3000 for development)"

must_haves:
  truths:
    - "User roles are enforced at database and middleware level"
    - "Auth.js integrates with Prisma for user storage"
    - "Session management persists across browser restarts"
    - "Role-based route protection works correctly"
  artifacts:
    - path: "src/lib/auth.ts"
      provides: "Auth.js configuration with Prisma adapter"
      exports: ["authOptions", "getServerSession"]
    - path: "src/middleware.ts"
      provides: "Route protection middleware"
      contains: "withAuth"
    - path: "src/types/auth.ts"
      provides: "Authentication type definitions"
      contains: "enum UserRole"
  key_links:
    - from: "src/lib/auth.ts"
      to: "prisma/schema.prisma"
      via: "Prisma adapter integration"
      pattern: "PrismaAdapter"
    - from: "src/middleware.ts"
      to: "src/lib/auth.ts"
      via: "Session validation"
      pattern: "getServerSession"
---

<objective>
Establish authentication foundation with role-based access control

Purpose: Create secure authentication system supporting user, supervisor, and superadmin roles with proper session management
Output: Auth.js configuration with Prisma integration and role-based middleware protection
</objective>

<execution_context>
../../../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Configure Auth.js with Prisma adapter and role support</name>
  <files>src/lib/auth.ts, src/types/auth.ts</files>
  <action>Setup Auth.js authentication with Prisma database adapter:
  - Install Auth.js (next-auth) and Prisma adapter packages
  - Configure credentials provider for email/password authentication
  - Setup Prisma adapter to store sessions and users in database
  - Define UserRole enum (USER, SUPERVISOR, SUPERADMIN) in types
  - Configure session callbacks to include user role and region
  - Setup JWT and session configuration for persistent login
  Use Auth.js v5 (latest) with Next.js 15 App Router compatibility</action>
  <verify>Auth.js configuration imports without errors, session callbacks work correctly, role information persists in session</verify>
  <done>Working Auth.js setup with Prisma adapter, role-based session management configured</done>
</task>

<task type="auto">
  <name>Expand database schema for authentication and roles</name>
  <files>prisma/schema.prisma</files>
  <action>Extend Prisma schema to support authentication and role management:
  - Add Auth.js required tables (Account, Session, VerificationToken)
  - Enhance User model with authentication fields (email, password, emailVerified, role, status, regionId)
  - Create Region model (id, name, description, createdAt)
  - Create UserStatus enum (PENDING_VERIFICATION, PENDING_APPROVAL, ACTIVE, REJECTED, SUSPENDED)
  - Add proper relations between User and Region
  - Generate and run migration to update database
  Follow Auth.js Prisma adapter schema requirements exactly</action>
  <verify>npx prisma migrate dev runs successfully, prisma studio shows updated schema, all relations work correctly</verify>
  <done>Database schema updated with authentication tables, user roles, and region management</done>
</task>

<task type="auto">
  <name>Create middleware for role-based route protection</name>
  <files>src/middleware.ts, src/lib/validations.ts</files>
  <action>Implement Next.js middleware for authentication and authorization:
  - Create middleware.ts to protect routes based on user roles
  - Define route patterns for different access levels (/supervisor/*, /superadmin/*, /dashboard/*)
  - Implement session validation using Auth.js getServerSession
  - Add redirect logic for unauthenticated users to login page
  - Create validation utilities for role checking and region access
  - Ensure middleware runs on all protected routes
  Use Next.js 15 middleware patterns with Auth.js integration</action>
  <verify>Protected routes redirect unauthenticated users, role-based access works correctly, middleware configuration applies to correct paths</verify>
  <done>Route protection middleware active, role-based access control enforced at application level</done>
</task>

</tasks>

<verification>
- Auth.js configuration works with Prisma adapter
- Database schema includes all authentication and role management tables
- Session management persists user login across browser restarts
- Middleware correctly protects routes based on user roles
- Role and region information available in session context
</verification>

<success_criteria>
- Complete authentication foundation with Auth.js and Prisma integration
- User role system (USER, SUPERVISOR, SUPERADMIN) implemented
- Route protection middleware active for role-based access control
- Database schema supports full authentication and regional supervision model
- Session management ready for user registration and login flows
</success_criteria>

<output>
After completion, create `.gsd/phases/01-foundation-approval-system/01-02-SUMMARY.md`
</output>