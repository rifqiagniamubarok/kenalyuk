---
phase: 08-discovery-page-remove-arrow-like-pass-add-tooltip-hide-available-count-simplify-card-and-detail-modal-add-card-swipe-animation
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/components/ProfileCard.tsx
  - src/app/(user)/discovery/page.tsx
autonomous: true

must_haves:
  truths:
    - 'Discovery card shows simplified summary: main/profile picture, name, and about'
    - 'Detail button on card opens modal instead of embedding all biodata in card'
    - 'Detail modal shows main picture, name, about, full biodata, and other photos in simple order'
  artifacts:
    - path: 'src/components/ProfileCard.tsx'
      provides: 'Simplified discovery summary card with detail trigger'
      contains: 'main image + name + about + Detail button'
    - path: 'src/app/(user)/discovery/page.tsx'
      provides: 'Detail modal state and full profile presentation'
      contains: 'modal with ordered content sections'
  key_links:
    - from: 'Detail button in ProfileCard'
      to: 'discovery modal open handler'
      via: 'callback prop from parent'
      pattern: 'onOpenDetail'
---

<objective>
Simplify discovery card surface and move full biodata into a dedicated detail modal.

Purpose: Keep the discovery card clean and focused while still allowing full profile inspection on demand.
Output: Card with main photo + name/about + detail button, plus modal showing full profile detail and photo gallery.
</objective>

<execution_context>
@../../../.github/skills/execute-plan/SKILL.md
@../../../.gsd/templates/summary.md
</execution_context>

<context>
@../../../.gsd/ROADMAP.md
@../../../.gsd/STATE.md
@../../../src/components/ProfileCard.tsx
@../../../src/app/(user)/discovery/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify discovery profile card content and add detail trigger</name>
  <files>src/components/ProfileCard.tsx</files>
  <action>
    Refactor card layout to prioritize:
    - main/profile picture
    - name
    - about snippet
    Add a `Detail` button on the card surface that triggers parent-provided detail handler.
    Remove dense inline biodata rendering from default card body.
  </action>
  <verify>On `/discovery`, card shows main photo + name + about + detail button with cleaner/simple layout</verify>
  <done>Card is simplified and no longer overloaded with full biodata</done>
</task>

<task type="auto">
  <name>Task 2: Build detail modal with ordered full profile content</name>
  <files>src/app/(user)/discovery/page.tsx, src/components/ProfileCard.tsx</files>
  <action>
    Add detail modal state in discovery page and wire `Detail` button from card.
    Modal content order must be:
    1) main/profile picture
    2) name
    3) about
    4) all biodata fields
    5) other photos section
    Keep visual structure simple and tidy; avoid adding extra tabs/complex navigation.
  </action>
  <verify>Click `Detail` from card and confirm modal opens with required ordered sections</verify>
  <done>Detail modal provides complete profile info while main card remains simple</done>
</task>

</tasks>

<verification>
- Discovery card surface remains compact and readable
- Full biodata and additional photos are accessible through Detail modal only
- Modal content order matches requested sequence
</verification>

<success_criteria>
08-02 is complete when discovery card is simplified and full profile detail is shown in a clean modal flow.
</success_criteria>

<output>
After completion, create `.gsd/phases/08-discovery-page-remove-arrow-like-pass-add-tooltip-hide-available-count-simplify-card-and-detail-modal-add-card-swipe-animation/08-02-SUMMARY.md`
</output>
