---
phase: 08-discovery-page-remove-arrow-like-pass-add-tooltip-hide-available-count-simplify-card-and-detail-modal-add-card-swipe-animation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(user)/discovery/page.tsx
  - src/components/ProfileCard.tsx
autonomous: true

must_haves:
  truths:
    - 'Discovery no longer supports arrow-key pass/like shortcuts'
    - 'Discovery header no longer exposes remaining/total candidate counts'
    - 'Pass/Like icons show tooltips and are rendered in a separate action card below the profile card'
  artifacts:
    - path: 'src/app/(user)/discovery/page.tsx'
      provides: 'Discovery action controls and hidden availability indicators'
      contains: 'tooltip-based pass/like actions below profile card'
    - path: 'src/components/ProfileCard.tsx'
      provides: 'Profile card content without embedded pass/like action footer'
      contains: 'showActions control for discovery usage'
  key_links:
    - from: 'src/app/(user)/discovery/page.tsx'
      to: 'src/components/ProfileCard.tsx'
      via: 'pass showActions=false and render action controls separately'
      pattern: 'showActions'
---

<objective>
Restructure discovery actions so pass/like are no longer keyboard-arrow driven and are shown as tooltip-enabled icon actions below the profile card.

Purpose: Simplify discovery interactions and avoid exposing candidate availability counts.
Output: Discovery page with explicit icon actions below card, tooltips added, arrow-key controls removed, and progress/count indicators removed.
</objective>

<execution_context>
@../../../.github/skills/execute-plan/SKILL.md
@../../../.gsd/templates/summary.md
</execution_context>

<context>
@../../../.gsd/ROADMAP.md
@../../../.gsd/STATE.md
@../../../src/app/(user)/discovery/page.tsx
@../../../src/components/ProfileCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove arrow-key pass/like shortcuts and candidate count indicators</name>
  <files>src/app/(user)/discovery/page.tsx</files>
  <action>
    Remove keyboard event handlers that trigger pass/like from left/right arrows.
    Remove progress/count UI that displays current index and total available profiles.
    Keep loading/error/no-profiles states intact.
  </action>
  <verify>Open `/discovery`; no arrow-key shortcut hint and no visible `x / total` availability counter are shown</verify>
  <done>Discovery no longer exposes candidate counts and no longer uses keyboard arrow actions</done>
</task>

<task type="auto">
  <name>Task 2: Add tooltip pass/like icon actions in separate card below profile card</name>
  <files>src/app/(user)/discovery/page.tsx, src/components/ProfileCard.tsx</files>
  <action>
    Render pass/like buttons in a dedicated section/card below the profile card (not inside profile card footer).
    Use icon-style buttons with tooltips (`Pass`, `Like`) and wire them to existing handlers.
    Disable actions consistently during actionLoading.
    Ensure profile card can render without embedded action buttons for discovery usage.
  </action>
  <verify>On `/discovery`, pass/like icon buttons appear below card in a separate container with tooltip labels</verify>
  <done>Action controls are moved below the profile card and tooltips are visible on pass/like icons</done>
</task>

</tasks>

<verification>
- No arrow-key pass/like behavior remains
- Discovery header does not reveal remaining/total user availability
- Pass/Like controls are tooltip-driven icon actions below the card
</verification>

<success_criteria>
08-01 is complete when discovery interactions are click-first, count-hidden, and action controls are relocated below the card with tooltips.
</success_criteria>

<output>
After completion, create `.gsd/phases/08-discovery-page-remove-arrow-like-pass-add-tooltip-hide-available-count-simplify-card-and-detail-modal-add-card-swipe-animation/08-01-SUMMARY.md`
</output>
