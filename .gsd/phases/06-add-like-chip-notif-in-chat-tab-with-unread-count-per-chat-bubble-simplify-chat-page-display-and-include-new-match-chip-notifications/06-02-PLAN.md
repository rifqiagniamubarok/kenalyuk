---
phase: 06-add-like-chip-notif-in-chat-tab-with-unread-count-per-chat-bubble-simplify-chat-page-display-and-include-new-match-chip-notifications
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/app/(user)/chat/page.tsx
  - src/app/(user)/matches/page.tsx
autonomous: true

must_haves:
  truths:
    - 'Chat page shows a simple conversation list: profile photo, name, last chat, and unread chip'
    - 'Conversation row displays unread chip count when there are unread messages'
    - 'New match with no chat is visually indicated and contributes to chat notification behavior'
  artifacts:
    - path: 'src/app/(user)/chat/page.tsx'
      provides: 'Chat inbox list page using matches API notification metadata'
      contains: 'profile photo, name, last chat, unread chip/new badge'
    - path: 'src/app/(user)/matches/page.tsx'
      provides: 'Compatibility handoff to new chat list entry'
      contains: '/chat links consistent with inbox flow'
  key_links:
    - from: 'src/app/(user)/chat/page.tsx'
      to: '/api/matches'
      via: 'fetch conversation summaries'
      pattern: 'fetch\\(\\s*["\'']\\/api\\/matches["\'']'
    - from: 'chat list item'
      to: '/chat/[matchId]'
      via: 'link per conversation'
      pattern: '/chat/${match.matchId}'
---

<objective>
Build a simplified chat list page showing profile photo, name, last chat, and per-conversation unread chip.

Purpose: Match requested minimal chat list UX and make unread/new-match state obvious before entering a room.
Output: `/chat` inbox UI with unread chips and new-match indicator integrated with matches metadata.
</objective>

<execution_context>
@../../../.github/skills/execute-plan/SKILL.md
@../../../.gsd/templates/summary.md
</execution_context>

<context>
@../../../.gsd/PROJECT.md
@../../../.gsd/ROADMAP.md
@../../../.gsd/STATE.md
@../../../src/app/(user)/chat/page.tsx
@../../../src/app/(user)/matches/page.tsx
@../../../src/app/api/matches/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace chat entry redirect with simplified chat inbox page</name>
  <files>src/app/(user)/chat/page.tsx</files>
  <action>
    Replace redirect-only `/chat` entry with client page that fetches `/api/matches` and renders a simple conversation list.
    For each row show exactly:
    - profile photo
    - display name
    - last chat text (fallback: "New match - start chatting")
    - chip badge with unread count when `unreadCount > 0`
    Add visual "new" indicator for `hasNoMessages=true` (e.g., chip "New").
    Keep click target to open `/chat/[matchId]`.
  </action>
  <verify>
    - `/chat` loads conversation list (not redirect)
    - Rows show photo, name, last chat preview
    - Unread chip appears only when `unreadCount > 0`
    - New-match row shows indicator when no messages exist
  </verify>
  <done>Chat page is simplified and exposes per-conversation unread/new-match state</done>
</task>

<task type="auto">
  <name>Task 2: Align matches page linking/wording with inbox-first chat flow</name>
  <files>src/app/(user)/matches/page.tsx</files>
  <action>
    Keep matches cards but align CTA behavior with new chat inbox expectations:
    - Ensure chat actions link to `/chat/[matchId]`
    - Keep copy consistent with new unread/new-match messaging
    Do not add extra complex UI; keep minimal adjustments needed for coherent flow.
  </action>
  <verify>
    - Matches chat CTA still opens room correctly
    - Copy/entry flow is consistent with new `/chat` inbox page
  </verify>
  <done>Matches and chat entry flows are consistent after simplified inbox introduction</done>
</task>

</tasks>

<verification>
- `/chat` renders simple list structure requested by user
- Per-chat unread chips appear correctly
- New matches with no messages are visibly indicated
- `/chat/[matchId]` navigation remains functional
</verification>

<success_criteria>
06-02 complete when `/chat` is a simplified inbox showing photo, name, last message, and unread chip/new indicator per conversation.
</success_criteria>

<output>
After completion, create `.gsd/phases/06-add-like-chip-notif-in-chat-tab-with-unread-count-per-chat-bubble-simplify-chat-page-display-and-include-new-match-chip-notifications/06-02-SUMMARY.md`
</output>
