---
phase: 06-add-like-chip-notif-in-chat-tab-with-unread-count-per-chat-bubble-simplify-chat-page-display-and-include-new-match-chip-notifications
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - src/app/(user)/layout.tsx
  - src/components/Navigation.tsx
  - src/app/api/matches/route.ts
autonomous: false

must_haves:
  truths:
    - 'Chat tab shows notification chip like Chat (3) for unread/new conversations'
    - 'Tab chip total includes unread messages plus new matches without chat'
    - 'Tab chip decreases after opening/reading conversations'
  artifacts:
    - path: 'src/components/Navigation.tsx'
      provides: 'Navigation item rendering with optional numeric badge/chip'
      contains: 'badge/chip next to Chat label'
    - path: 'src/app/(user)/layout.tsx'
      provides: 'Chat tab count fetching and menu item injection'
      contains: 'chat notification count for menu item'
    - path: 'src/app/api/matches/route.ts'
      provides: 'Optional aggregate count support for chat tab badge'
      contains: 'total unread/new matches count data'
  key_links:
    - from: 'src/app/(user)/layout.tsx'
      to: 'src/components/Navigation.tsx'
      via: 'menu items include chat badge data'
      pattern: 'menuItems'
    - from: 'chat tab chip'
      to: '/api/matches'
      via: 'aggregate from unreadCount + hasNoMessages'
      pattern: 'unreadCount|hasNoMessages'
---

<objective>
Add chat-tab notification chip (e.g., Chat (3)) that aggregates unread messages and new matches without chat.

Purpose: Make high-level chat attention state visible from primary navigation, including new matches with no messages.
Output: Chat navigation item displays dynamic chip count and updates with read-state changes.
</objective>

<execution_context>
@../../../.github/skills/execute-plan/SKILL.md
@../../../.gsd/templates/summary.md
</execution_context>

<context>
@../../../.gsd/PROJECT.md
@../../../.gsd/ROADMAP.md
@../../../.gsd/STATE.md
@../../../src/app/(user)/layout.tsx
@../../../src/components/Navigation.tsx
@../../../src/app/api/matches/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add chat tab aggregate chip count to user navigation</name>
  <files>src/app/(user)/layout.tsx, src/components/Navigation.tsx, src/app/api/matches/route.ts</files>
  <action>
    Extend menu item model to support optional `badgeCount`.
    Compute chat tab count as:
    - sum of unread message counts across matches
    - plus count of matches where `hasNoMessages=true`
    Render chat tab label with chip (e.g., Chat (3)) when total > 0.
    Keep non-chat tabs unchanged.
    If aggregate data is easier from API, add compact summary field in `/api/matches` response while preserving backward compatibility.
  </action>
  <verify>
    - Navigation shows `Chat (N)` or chip equivalent when total > 0
    - Count includes unread messages + new no-message matches
    - No chip shown when total is 0
  </verify>
  <done>Chat tab chip accurately represents unread/new conversation total</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Unread/new-match chat notification chips on both chat page rows and navigation chat tab</what-built>
  <how-to-verify>
    1. Open `/chat` and confirm each conversation row shows photo, name, last chat, and unread chip when applicable.
    2. Confirm rows for matches with zero messages show a "new" indicator.
    3. In main navigation, confirm Chat tab shows count chip (e.g., `Chat (3)`) when unread/new items exist.
    4. Open one unread conversation (`/chat/[matchId]`), then return to `/chat` and verify that unread count decreases.
    5. Confirm chat-tab chip total updates after read actions and reaches hidden/zero state when fully read.
  </how-to-verify>
  <resume-signal>Type "approved" to continue or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Chat tab chip displays aggregate unread/new-match count
- Chat list row chips display unread counts and new-match indicator
- Read-state updates reduce counts predictably
- UI remains minimal (photo, name, last chat, chip)
</verification>

<success_criteria>
06-03 complete when chat list and navigation both show accurate unread/new-match chips, with totals updating after conversations are read.
</success_criteria>

<output>
After completion, create `.gsd/phases/06-add-like-chip-notif-in-chat-tab-with-unread-count-per-chat-bubble-simplify-chat-page-display-and-include-new-match-chip-notifications/06-03-SUMMARY.md`
</output>
