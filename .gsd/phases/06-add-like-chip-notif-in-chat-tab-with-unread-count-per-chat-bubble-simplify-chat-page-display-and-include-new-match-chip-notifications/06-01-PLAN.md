---
phase: 06-add-like-chip-notif-in-chat-tab-with-unread-count-per-chat-bubble-simplify-chat-page-display-and-include-new-match-chip-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/matches/route.ts
  - src/app/api/messages/[matchId]/route.ts
  - src/lib/useChat.ts
autonomous: true

must_haves:
  truths:
    - 'System calculates unread message counts per chat conversation for current user'
    - 'Unread messages are marked read when user opens the chat room'
    - 'New matches with zero messages are surfaced as chat notifications'
  artifacts:
    - path: 'src/app/api/matches/route.ts'
      provides: 'Match list payload with unread count and hasNoMessages flag'
      exports: ['GET']
    - path: 'src/app/api/messages/[matchId]/route.ts'
      provides: 'Message history endpoint that marks incoming unread messages as read'
      exports: ['GET']
    - path: 'src/lib/useChat.ts'
      provides: 'Client chat hook aligned with read-state API response'
      contains: 'fetch /api/messages/[matchId] response handling'
  key_links:
    - from: 'src/app/api/matches/route.ts'
      to: 'prisma.message'
      via: 'unread aggregation where senderId != current user and isRead=false'
      pattern: 'prisma\\.message\\.(count|groupBy|findMany)'
    - from: 'src/app/api/messages/[matchId]/route.ts'
      to: 'prisma.message'
      via: 'mark as read updateMany before returning history'
      pattern: 'updateMany'
    - from: 'src/lib/useChat.ts'
      to: '/api/messages/[matchId]'
      via: 'history fetch'
      pattern: 'fetch\\(`/api/messages/\\$\\{matchId\\}`\\)'
---

<objective>
Implement unread/new-match notification data contract for chat so UI can show per-conversation chips and tab-level counts.

Purpose: Provide reliable server-side unread counting and read-state transitions needed for chat notification UX.
Output: Matches API extended with chat notification metadata and message history endpoint marking messages as read.
</objective>

<execution_context>
@../../../.github/skills/execute-plan/SKILL.md
@../../../.gsd/templates/summary.md
</execution_context>

<context>
@../../../.gsd/PROJECT.md
@../../../.gsd/ROADMAP.md
@../../../.gsd/STATE.md
@../../../src/app/api/matches/route.ts
@../../../src/app/api/messages/[matchId]/route.ts
@../../../prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend matches API with unread counters and new-match signal</name>
  <files>src/app/api/matches/route.ts</files>
  <action>
    Update match response shape to include:
    - `unreadCount` per match (messages where sender is the other user and `isRead=false`)
    - `hasNoMessages` boolean (`true` when no messages exist yet in the match)
    - `lastMessage` summary (content + createdAt + senderId) for chat list display
    Keep existing fields for backward compatibility where practical.
    Ensure query remains scoped to current authenticated ACTIVE user.
  </action>
  <verify>
    - GET `/api/matches` returns `unreadCount`, `hasNoMessages`, and `lastMessage` per match
    - Unread count excludes messages sent by current user
    - Matches with no messages have `hasNoMessages=true`
  </verify>
  <done>Matches API provides all chat notification data needed by Phase 06 UI</done>
</task>

<task type="auto">
  <name>Task 2: Mark messages as read when opening chat room</name>
  <files>src/app/api/messages/[matchId]/route.ts, src/lib/useChat.ts</files>
  <action>
    In message history GET endpoint, after participant validation and before returning messages, mark unread incoming messages as read:
    - `where: { matchId, senderId: { not: currentUserId }, isRead: false }`
    - `data: { isRead: true }`
    Return stable response shape for client hook.
    Update `useChat` only as needed so it works with any added response metadata.
  </action>
  <verify>
    - Opening `/chat/[matchId]` triggers read update for incoming unread messages
    - Re-fetching `/api/matches` after opening chat reduces that match `unreadCount`
    - No regression in existing chat message history rendering
  </verify>
  <done>Read-state transitions work and unread counts can decrement correctly</done>
</task>

</tasks>

<verification>
- Matches API includes unread/new-match metadata
- Message history endpoint updates `isRead` for incoming unread messages
- Chat hook still loads messages successfully
- TypeScript/build checks for touched files pass (ignoring known unrelated project-level build issues)
</verification>

<success_criteria>
06-01 complete when backend returns per-chat unread counts + new-match signal and unread messages are marked read when chat room is opened.
</success_criteria>

<output>
After completion, create `.gsd/phases/06-add-like-chip-notif-in-chat-tab-with-unread-count-per-chat-bubble-simplify-chat-page-display-and-include-new-match-chip-notifications/06-01-SUMMARY.md`
</output>
