---
phase: 04-user-interface-redesign-and-simplification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/PhotoUpload.tsx
  - src/app/api/upload/order/route.ts
autonomous: true

must_haves:
  truths:
    - 'Users can drag and drop photos to reorder'
    - 'First photo becomes profile picture across platform'
    - 'Photo order persists after page refresh'
    - 'Reordering is smooth and responsive'
  artifacts:
    - path: 'src/components/PhotoUpload.tsx'
      provides: 'Drag-and-drop photo reordering component'
      contains: '@dnd-kit functionality, photo list with drag handles'
    - path: 'src/app/api/upload/order/route.ts'
      provides: 'Photo ordering API endpoint'
      exports: ['PUT']
    - path: 'package.json'
      provides: '@dnd-kit dependencies'
      contains: '@dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities'
  key_links:
    - from: 'src/components/PhotoUpload.tsx'
      to: '/api/upload/order'
      via: 'fetch PUT request'
      pattern: 'fetch.*\\/api\\/upload\\/order.*PUT'
    - from: 'Photo reorder action'
      to: 'photoUrls array order'
      via: 'Prisma update'
      pattern: 'prisma\\.user\\.update.*photoUrls'
---

<objective>
Implement drag-and-drop photo reordering system where users can customize their photo display order, with the first photo automatically becoming their profile picture.

Purpose: Allow users to control their photo presentation for better match visibility.
Output: PhotoUpload component with drag-and-drop reordering and API endpoint to persist order changes.
</objective>

<execution_context>
@../../../.github/skills/execute-plan/SKILL.md
@../../../.gsd/templates/summary.md
</execution_context>

<context>
@../../../.gsd/PROJECT.md
@../../../.gsd/ROADMAP.md
@../../../.gsd/STATE.md
@../../../src/components/PhotoUpload.tsx
@../../../prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @dnd-kit and implement drag-and-drop photo reordering</name>
  <files>package.json, src/components/PhotoUpload.tsx</files>
  <action>
    1. Install @dnd-kit packages:
    npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
    
    2. Update src/components/PhotoUpload.tsx to add drag-and-drop:
    - Import: { DndContext, closestCenter, KeyboardSensor, PointerSensor, useSensor, useSensors } from '@dnd-kit/core'
    - Import: { arrayMove, SortableContext, sortableKeyboardCoordinates, verticalListSortingStrategy } from '@dnd-kit/sortable'
    - Import: { useSortable } from '@dnd-kit/sortable'
    - Import: { CSS } from '@dnd-kit/utilities'
    
    3. Update PhotoUpload component:
    - Wrap photo list with DndContext with closestCenter collision detection
    - Configure sensors for mouse and touch: PointerSensor, KeyboardSensor
    - Use SortableContext with activeId state and verticalListSortingStrategy
    - Create SortablePhoto sub-component that uses useSortable hook
    - SortablePhoto shows drag handle, photo preview, and delete button
    - Implement handleDragEnd: setPhotos(arrayMove(...))
    
    4. Photo list styling:
    - Responsive grid: grid-cols-2 md:grid-cols-3 lg:grid-cols-4
    - Each photo card: rounded corners, border, hover effects
    - Drag handle: visible on hover, cursor grab/grabbing
    - Drag overlay preview
    
    5. Visual indicator:
    - Show "#1" or "Profile Picture" label on first photo
    - Subtle styling difference for first photo
  </action>
  <verify>
    - @dnd-kit packages in package.json
    - PhotoUpload renders draggable photo list
    - Can drag photos to different positions
    - Drag handle visible and responsive
    - First photo shows "Profile Picture" label
    - No TypeScript errors
    - Component renders without console errors
  </verify>
  <done>Drag-and-drop photo reordering UI complete</done>
</task>

<task type="auto">
  <name>Task 2: Create PUT /api/upload/order endpoint to persist photo order</name>
  <files>src/app/api/upload/order/route.ts</files>
  <action>
    1. Create src/app/api/upload/order/route.ts:
    
    2. Implement PUT handler:
    - Authenticate user (required)
    - Body: { photoIds: [string array] } or { photoUrls: [string array] }
    - Validate: user has exactly these photos (security check)
    - Update User.photoUrls array with new order
    - Return: { success: true, photoUrls: [...], profilePicture: first_url }
    
    3. Error handling:
    - 401: Not authenticated
    - 400: Invalid photoIds or missing body
    - 403: User attempting to reorder photos they don't own
    - 404: Photo not found for user
    - 500: Database error
    
    4. Implementation details:
    - Get current user from session
    - Validate photoIds length matches current photoUrls count
    - Validate each photoId exists in user's current photos
    - Create Prisma update:
      prisma.user.update({
        where: { id: userId },
        data: { photoUrls: newOrder }
      })
    - Return updated user with photoUrls in new order
    
    5. Response format:
    {
      success: true,
      photoUrls: [...reordered urls...],
      profilePicture: first_url
    }
  </action>
  <verify>
    - PUT /api/upload/order accepts photoIds array
    - Validates user ownership of photos
    - Updates database with new order
    - Returns updated photoUrls in correct order
    - First photo marked as profilePicture
    - Error cases handled (401, 400, 403, 404, 500)
    - TypeScript compiles clean
  </verify>
  <done>Photo ordering API endpoint persists reorder changes to database</done>
</task>

</tasks>

<verification>
- Installation: @dnd-kit packages in package.json
- UI: PhotoUpload shows draggable photo list with drag handles
- Drag: Photos can be reordered by dragging
- Labels: First photo marked as "Profile Picture"
- API: PUT /api/upload/order accepts and persists new photo order
- Persistence: Photos maintain order after page refresh
- Security: Validates user ownership before updating
- Build: npm run build succeeds
- Types: TypeScript clean
- Requirements Met: UI-03 (photo ordering), UI-04 (first photo = profile picture)
</verification>

<success_criteria>
Phase 04-02 complete when users can drag-and-drop reorder photos, first photo displays as profile picture with visual indicator, and photo order persists after refresh via API endpoint.
</success_criteria>

<output>
After completion, create `.gsd/phases/04-user-interface-redesign-and-simplification/04-02-SUMMARY.md`
</output>
