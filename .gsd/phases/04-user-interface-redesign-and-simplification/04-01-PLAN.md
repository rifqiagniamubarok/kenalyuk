---
phase: 04-user-interface-redesign-and-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(user)/layout.tsx
  - src/components/Navigation.tsx
  - package.json
autonomous: true

must_haves:
  truths:
    - 'Navigation shows only 3 menus: Profile, Discovery, Chat'
    - 'All menus use Lucide React icons instead of emoji'
    - 'Unapproved (PENDING/REJECTED) users see only Profile menu'
    - 'Approved (ACTIVE) users see all 3 menus'
    - 'Icons are consistent size and styling across pages'
  artifacts:
    - path: 'src/app/(user)/layout.tsx'
      provides: 'Updated navigation with 3 menu items'
      contains: 'lucide-react imports, conditional rendering by user.status'
    - path: 'package.json'
      provides: 'lucide-react dependency'
      contains: 'lucide-react@latest'
  key_links:
    - from: 'src/app/(user)/layout.tsx'
      to: 'lucide-react icons'
      via: 'import {}'
      pattern: 'import.*User.*Heart.*MessageCircle.*from.*lucide-react'
    - from: 'Navigation menu'
      to: 'user.status'
      via: 'conditional render'
      pattern: 'status.*ACTIVE|PENDING|REJECTED'
---

<objective>
Simplify navigation from 6 menus to 3 core menus (Profile, Discovery, Chat) and replace all emoji icons with Lucide React icons.

Purpose: Streamline user navigation to focus on essential features and improve UI consistency.
Output: Updated layout with 3 menus, Lucide icons, and conditional visibility based on approval status.
</objective>

<execution_context>
@../../../.github/skills/execute-plan/SKILL.md
@../../../.gsd/templates/summary.md
</execution_context>

<context>
@../../../.gsd/PROJECT.md
@../../../.gsd/ROADMAP.md
@../../../.gsd/STATE.md
@../../../src/app/(user)/layout.tsx
@../../../package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lucide-react and update navigation with 3 menus and Lucide icons</name>
  <files>package.json, src/app/(user)/layout.tsx</files>
  <action>
    1. Install lucide-react: npm install lucide-react
    
    2. Update src/app/(user)/layout.tsx:
    - Import Lucide icons: import { User, Heart, MessageCircle } from 'lucide-react'
    - Replace userMenuItems array with 3 items: Profile, Discovery, Chat
    - Replace emoji icons 💕, 📝, 💬 with Lucide icons: Heart, MessageCircle
    - Keep consistent icon styling: w-5 h-5, text-current
    - Navigation order: Profile → Discovery → Chat
    - Update hrefs: /profile, /discovery, /chat
    - Keep error page link as-is
    
    3. Icon mapping:
    - Profile: User icon
    - Discovery: Heart icon
    - Chat: MessageCircle icon
    
    4. Style icons with consistent sizing (w-5 h-5) and color
  </action>
  <verify>
    npm list lucide-react shows package installed
    src/app/(user)/layout.tsx imports Lucide icons correctly
    Navigation displays only Profile, Discovery, Chat with Lucide icons
    npm run build succeeds
    No TypeScript errors
  </verify>
  <done>Navigation shows 3 menus with Lucide icons instead of emoji</done>
</task>

<task type="auto">
  <name>Task 2: Implement conditional navigation based on user approval status</name>
  <files>src/app/(user)/layout.tsx</files>
  <action>
    1. Conditional rendering by user.status:
    - If status is PENDING or REJECTED: Show only Profile menu + error page link
    - If status is ACTIVE: Show all 3 menus (Profile, Discovery, Chat) + error page link
    
    2. Implementation:
    - Get user status from session.user.status
    - Filter userMenuItems array based on status before rendering
    - PENDING/REJECTED users: [Profile item only]
    - ACTIVE users: [Profile, Discovery, Chat]
    
    3. Message/warning (optional but nice):
    - Consider adding text hint below menu for pending users: "Complete your profile to unlock Discovery and Chat"
    
    4. Ensure mobile responsive menu also updates conditionally
  </action>
  <verify>
    - Login with PENDING user: See only Profile menu
    - Login with REJECTED user: See only Profile menu
    - Login with ACTIVE user: See Profile, Discovery, Chat menus
    - All icons render correctly
    - Navigation styling consistent
    - No console errors
  </verify>
  <done>Unapproved users see only Profile menu, approved users see all 3 menus</done>
</task>

</tasks>

<verification>
- Installation: lucide-react package in package.json and node_modules
- Navigation: Displays 3 menus (Profile, Discovery, Chat) for approved users
- Icons: Lucide React icons (User, Heart, MessageCircle) display correctly
- Conditional: PENDING/REJECTED users see only Profile menu
- Styling: Icons consistent size and styling (w-5 h-5)
- Build: npm run build succeeds without errors
- Types: TypeScript compilation clean
- Requirements Met: UI-02 (simplified navigation), UI-07 (Lucide icons), UI-08 (conditional visibility)
</verification>

<success_criteria>
Phase 04-01 complete when navigation shows exactly 3 menus for approved users using Lucide icons, unapproved users see only Profile, all icons are consistent, and build succeeds.
</success_criteria>

<output>
After completion, create `.gsd/phases/04-user-interface-redesign-and-simplification/04-01-SUMMARY.md`
</output>
