---
phase: 02-discovery-&-matching-core
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - src/app/api/matches/route.ts
  - src/app/(user)/matches/page.tsx
  - src/app/api/likes/sent/route.ts
  - src/app/(user)/likes/page.tsx
autonomous: true

must_haves:
  truths:
    - 'Users can view their active matches'
    - "Users can view likes they've sent"
    - 'Match list shows most recent activity first'
  artifacts:
    - path: 'src/app/api/matches/route.ts'
      provides: 'Matches list API'
      exports: ['GET']
    - path: 'src/app/(user)/matches/page.tsx'
      provides: 'Matches list UI'
      min_lines: 40
    - path: 'src/app/api/likes/sent/route.ts'
      provides: 'Sent likes API'
      exports: ['GET']
    - path: 'src/app/(user)/likes/page.tsx'
      provides: 'Like history UI'
      min_lines: 30
  key_links:
    - from: 'src/app/(user)/matches/page.tsx'
      to: '/api/matches'
      via: 'fetch on mount'
      pattern: 'fetch.*api/matches'
---

<objective>
Create matches list and like history views.

Purpose: Allow users to see their matches and track likes they've sent.
Output: Working matches and likes pages with profile data.
</objective>

<execution_context>
@../../../.github/skills/execute-plan/SKILL.md
@../../../.gsd/templates/summary.md
</execution_context>

<context>
@../../../.gsd/PROJECT.md
@../../../.gsd/ROADMAP.md
@../../../.gsd/STATE.md
@../../../prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Create matches API and UI</name>
  <files>src/app/api/matches/route.ts, src/app/(user)/matches/page.tsx</files>
  <action>
    Create GET /api/matches endpoint:
    - Requires authentication and ACTIVE status
    - Query Match records where userId is either user1Id or user2Id
    - Include the other user's data (name, photos, city, age)
    - Sort by updatedAt DESC (most recent first)
    - Return array of matches with matched user data
    
    Create matches page:
    - Client component fetching /api/matches
    - Grid layout of match cards (NextUI Card)
    - Each card: user photo, name, age, city, "Chat" button
    - Chat button links to /chat/[matchId] (to be built in Phase 03)
    - Empty state: "No matches yet" with link to discovery
    - Loading state: NextUI Skeleton cards
    
    Use: Card, CardBody, Image, Button, Skeleton
  </action>
  <verify>API returns matches, page displays match cards correctly</verify>
  <done>Users can view their active matches</done>
</task>

<task type="auto">
  <name>Create like history API and UI</name>
  <files>src/app/api/likes/sent/route.ts, src/app/(user)/likes/page.tsx</files>
  <action>
    Create GET /api/likes/sent endpoint:
    - Requires authentication and ACTIVE status
    - Query Like records where userId equals current user
    - Include liked user data (name, photos, city, age)
    - Check if liked back: LEFT JOIN Like as reverse
    - Return: array with user data and likedBack boolean
    - Sort by createdAt DESC
    
    Create likes page:
    - Client component fetching /api/likes/sent
    - List of liked profiles with status indicator
    - Each item: user photo, name, age, status chip (Matched/Pending)
    - If matched: "View Match" button linking to /matches
    - Empty state: "No likes sent yet" with link to discovery
    - Loading state: NextUI Skeleton
    
    Use: Card, Chip, Image, Divider, Skeleton
  </action>
  <verify>API returns sent likes with status, page displays correctly</verify>
  <done>Users can view their like history with match status</done>
</task>

<task type="auto">
  <name>Add navigation links</name>
  <files>src/app/(user)/layout.tsx</files>
  <action>
    Update user layout navigation to include:
    - Dashboard link (existing)
    - Discovery link (/discovery) - with heart icon
    - Matches link (/matches) - with people icon
    - Likes link (/likes) - with clock/history icon
    - Biodata link (existing)
    - Photos link (existing)
    
    Use NextUI Tabs or navigation buttons
    Highlight active route
    Mobile: bottom navigation bar
    Desktop: top navigation tabs
  </action>
  <verify>Navigation shows all links, active state works</verify>
  <done>Users can navigate between discovery, matches, and likes</done>
</task>

</tasks>

<verification>
- GET /api/matches returns user matches
- Matches page displays match cards
- GET /api/likes/sent returns sent likes with status
- Likes page shows like history
- Navigation links work
- Loading/empty states display correctly
- TypeScript compiles without errors
</verification>

<success_criteria>
Matches and history views complete when users can view matches list, see like history with match status, and navigate between discovery features. Requirements DISC-04 and DISC-05 satisfied.
</success_criteria>

<output>
After completion, create `.gsd/phases/02-discovery-&-matching-core/02-04-SUMMARY.md`
</output>
