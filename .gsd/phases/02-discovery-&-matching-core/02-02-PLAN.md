---
phase: 02-discovery-&-matching-core
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/app/api/discovery/route.ts
  - src/app/(user)/discovery/page.tsx
  - src/components/ProfileCard.tsx
  - src/lib/discovery.ts
autonomous: true

must_haves:
  truths:
    - "Active users see discovery feed with profiles"
    - "Feed filters by gender, age range, and region preferences"
    - "Already liked/passed profiles excluded from feed"
  artifacts:
    - path: "src/app/api/discovery/route.ts"
      provides: "Discovery API with filtering logic"
      exports: ["GET"]
    - path: "src/app/(user)/discovery/page.tsx"
      provides: "Discovery feed UI"
      min_lines: 50
  key_links:
    - from: "src/app/(user)/discovery/page.tsx"
      to: "/api/discovery"
      via: "fetch in useEffect"
      pattern: "fetch.*api/discovery"
---

<objective>
Create discovery feed that shows filtered profiles to active users.

Purpose: Enable users to browse potential matches based on their preferences.
Output: Working discovery page with profile cards that respect filters and exclusions.
</objective>

<execution_context>
@../../../.github/skills/execute-plan/SKILL.md
@../../../.gsd/templates/summary.md
</execution_context>

<context>
@../../../.gsd/PROJECT.md
@../../../.gsd/ROADMAP.md
@../../../.gsd/STATE.md
@../../../prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Create discovery API endpoint with filtering</name>
  <files>src/app/api/discovery/route.ts, src/lib/discovery.ts</files>
  <action>
    Create GET /api/discovery endpoint that:
    - Requires authentication and ACTIVE status
    - Fetches user biodata to get preferences (gender, age range, region)
    - Queries User model with filters: opposite gender, ACTIVE status, age range, region preference
    - Excludes current user, already liked users (Like table), already passed users (Pass table)
    - Includes user photos (photo1-photo9 fields)
    - Returns max 20 profiles, randomized order
    - Returns: id, name, age, city, region, education, occupation, about, photos array
    
    Create src/lib/discovery.ts helpers:
    - calculateAge(dateOfBirth)
    - buildDiscoveryFilters(user biodata)
    
    Handle: no biodata (400), no profiles (empty array), errors (500)
  </action>
  <verify>npm run build succeeds, API returns filtered profiles for ACTIVE users</verify>
  <done>Discovery API returns filtered profiles based on preferences</done>
</task>

<task type="auto">
  <name>Create ProfileCard component</name>
  <files>src/components/ProfileCard.tsx</files>
  <action>
    Create ProfileCard with NextUI Card:
    - Props: profile data, onLike, onPass callbacks, showActions boolean
    - Photo carousel with NextUI Image (cycle through photo1-photo9)
    - Profile info overlay: name, age, city, region, education, occupation
    - Expandable About section (NextUI Accordion)
    - Action buttons (if showActions): Pass (red X icon), Like (green heart icon)
    - Responsive: mobile full width, desktop max-width 400px
    - Animations with framer-motion (fade-in, photo transitions)
    
    Use: Card, CardBody, CardFooter, Image, Button, Accordion, Chip
    Handle missing data gracefully
  </action>
  <verify>Component builds, TypeScript types correct, handles missing photos</verify>
  <done>ProfileCard displays user profiles with photos and info</done>
</task>

<task type="auto">
  <name>Create discovery page UI</name>
  <files>src/app/(user)/discovery/page.tsx</files>
  <action>
    Create client component with:
    - State: profiles array, currentIndex, loading, error, noMoreProfiles
    - useEffect: fetch /api/discovery on mount
    - UI: header, ProfileCard display, pass/like buttons, profile counter
    - handleLike/handlePass: advance to next profile (currentIndex++)
    - Keyboard shortcuts: left arrow = pass, right arrow = like
    - Loading state: NextUI Spinner
    - Error state: Card with retry button
    - Empty state: "No profiles available" message
    - Responsive: mobile full screen with fixed bottom buttons, desktop centered
    
    Use: Card, Spinner, Button, Progress for counter
  </action>
  <verify>Page renders, fetches profiles, displays cards, keyboard shortcuts work</verify>
  <done>Discovery page allows users to browse profiles</done>
</task>

</tasks>

<verification>
- API endpoint returns filtered profiles
- Discovery page displays profile cards
- Filters working (gender, age, region, exclusions)
- Buttons and keyboard shortcuts functional
- Loading/error/empty states work
- Responsive on mobile and desktop
- TypeScript compiles without errors
</verification>

<success_criteria>
Discovery feed complete when user can view filtered profiles, navigate with buttons/keyboard, and already-interacted profiles are excluded. Requirements DISC-01 and DISC-06 satisfied.
</success_criteria>

<output>
After completion, create `.gsd/phases/02-discovery-&-matching-core/02-02-SUMMARY.md`
</output>
