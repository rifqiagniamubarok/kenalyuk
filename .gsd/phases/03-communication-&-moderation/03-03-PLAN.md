---
phase: 03-communication-&-moderation
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/app/(user)/chat/[matchId]/page.tsx
  - src/lib/useChat.ts
  - src/components/ChatMessage.tsx
  - src/app/(user)/layout.tsx
autonomous: true

must_haves:
  truths:
    - Users can send and receive messages in real-time
    - Message history loads when opening chat room
    - Typing indicators show when other user is typing
    - Chat UI is mobile-responsive and accessible
    - Messages are marked as read automatically
  artifacts:
    - src/app/(user)/chat/[matchId]/page.tsx - Chat room page
    - src/lib/useChat.ts - Custom hook for chat logic
    - src/components/ChatMessage.tsx - Message component
  key_links:
    - Chat page fetches messages from GET /api/messages/[matchId]
    - Messages sent via POST /api/messages
    - Real-time updates received via SSE connection to /api/socket
---

<objective>
Create user-facing chat UI with real-time messaging

Purpose: Enable matched users to have conversations with typing indicators and live updates
Output: Complete chat interface with message history, send functionality, and real-time streaming
</objective>

<execution_context>
../../../.github/skills/execute-plan/SKILL.md
</execution_context>

<context>
@.gsd/phases/03-communication-&-moderation/03-02-SUMMARY.md
@.gsd/phases/02-discovery-&-matching-core/02-04-SUMMARY.md
@src/app/(user)/matches/page.tsx
@src/components/ProfileCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Create useChat custom hook for chat logic</name>
  <files>src/lib/useChat.ts</files>
  <action>
  Create src/lib/useChat.ts custom hook:
  - Accept matchId parameter
  - State: messages (Message[]), loading (boolean), error (string | null), isTyping (boolean)
  - On mount: fetch message history from GET /api/messages/[matchId]
  - Establish SSE connection to /api/socket
  - Listen for 'message' events, append to messages array if matchId matches
  - Provide sendMessage(content: string) function that POSTs to /api/messages
  - Provide setTyping(typing: boolean) function for typing indicator
  - Clean up SSE connection on unmount
  - Return: {messages, loading, error, sendMessage, isTyping}
  
  Follow patterns from Phase 02 components for loading/error state management.
  Use fetch() with EventSource for SSE connection.
  </action>
  <verify>Import useChat in test component, verify messages load and sendMessage works</verify>
  <done>useChat hook manages message state, SSE connection, and send functionality</done>
</task>

<task type="auto">
  <name>Create ChatMessage component and chat room page</name>
  <files>src/components/ChatMessage.tsx, src/app/(user)/chat/[matchId]/page.tsx</files>
  <action>
  Create src/components/ChatMessage.tsx:
  - Accept props: {message: Message, isOwn: boolean}
  - Use NextUI Card for message bubble
  - Right-align if isOwn, left-align otherwise
  - Show sender name, message content, timestamp
  - Different background color for own vs other messages
  - Mobile-responsive with max-width and proper spacing
  
  Create src/app/(user)/chat/[matchId]/page.tsx:
  - Extract matchId from params
  - Use useChat(matchId) hook
  - Show loading skeleton while fetching messages
  - Render message list using ChatMessage component
  - Show typing indicator when other user typing
  - Message input with NextUI Input and Button
  - Auto-scroll to bottom on new messages
  - Handle empty state (no messages yet)
  - Error state with retry button
  
  Follow patterns from src/app/(user)/discovery/page.tsx for NextUI components and loading states.
  Use flex-col-reverse for auto-scroll to bottom behavior.
  </action>
  <verify>Navigate to /chat/[matchId], see message history, send message appears in list</verify>
  <done>Chat UI displays messages, accepts new messages, shows typing indicators</done>
</task>

<task type="auto">
  <name>Update navigation to include chat access</name>
  <files>src/app/(user)/layout.tsx</files>
  <action>
  Update src/app/(user)/layout.tsx navigation:
  - Navigation to chat rooms is via /matches page (already has Chat button per match)
  - No separate "Chat" navigation item needed (users access via matches list)
  - Verify existing navigation structure supports chat flow
  - Document in code comment that chat access is through /matches page
  
  No changes needed if /matches page already links to /chat/[matchId].
  </action>
  <verify>Check /matches page has Chat button that links to /chat/[matchId]</verify>
  <done>Navigation structure supports chat access through matches list</done>
</task>

</tasks>

<verification>
- useChat hook manages messages, SSE, and sending
- Chat page displays message history
- Users can send new messages
- Real-time updates appear instantly
- Typing indicators work
- Mobile-responsive UI
- Navigation supports chat access
</verification>

<success_criteria>
Matched users can have real-time conversations with message history, typing indicators, and smooth UX on mobile and desktop.
</success_criteria>

<output>
After completion, create `.gsd/phases/03-communication-&-moderation/03-03-SUMMARY.md`
</output>
