---
phase: 05-adjust-send-email-smtp-env-configuration-add-forgot-password-and-verify-password-for-register-send-email-notification-after-supervisor-approval-and-add-function-to-read-html-format
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - prisma/schema.prisma
  - src/lib/password-reset.ts
  - src/app/api/auth/forgot-password/route.ts
  - src/app/api/auth/reset-password/route.ts
  - src/templates/emails/reset-password.html
autonomous: true

must_haves:
  truths:
    - 'Users can request a password reset email from forgot-password endpoint'
    - 'Reset links are time-limited and one-time-use'
    - 'Submitting a valid reset token updates the user password securely'
  artifacts:
    - path: 'prisma/schema.prisma'
      provides: 'Password reset token persistence model'
      contains: 'model PasswordResetToken'
    - path: 'src/app/api/auth/forgot-password/route.ts'
      provides: 'Endpoint to issue reset token and send reset email'
      exports: ['POST']
    - path: 'src/app/api/auth/reset-password/route.ts'
      provides: 'Endpoint to validate token and set new password'
      exports: ['POST']
    - path: 'src/templates/emails/reset-password.html'
      provides: 'HTML template for password reset email'
      contains: '{{resetUrl}}'
  key_links:
    - from: 'src/app/api/auth/forgot-password/route.ts'
      to: 'src/lib/password-reset.ts'
      via: 'token generation + email dispatch'
      pattern: 'sendPasswordResetEmail|createPasswordResetToken'
    - from: 'src/app/api/auth/reset-password/route.ts'
      to: 'prisma.passwordResetToken'
      via: 'token validation and invalidation'
      pattern: 'prisma\\.passwordResetToken\\.(find|delete|update|create)'
    - from: 'src/lib/password-reset.ts'
      to: 'src/templates/emails/reset-password.html'
      via: 'renderEmailTemplate("reset-password", ... )'
      pattern: 'renderEmailTemplate\\(\\s*["\'']reset-password["\'']'
---

<objective>
Add a secure forgot-password and reset-password backend flow with token storage and HTML reset email delivery.

Purpose: Enable account recovery without manual admin intervention while preserving security guarantees.
Output: Prisma token model + forgot/reset API routes + password reset email template and sender logic.
</objective>

<execution_context>
@../../../.github/skills/execute-plan/SKILL.md
@../../../.gsd/templates/summary.md
</execution_context>

<context>
@../../../.gsd/PROJECT.md
@../../../.gsd/ROADMAP.md
@../../../.gsd/STATE.md
@../../../prisma/schema.prisma
@../../../src/lib/password.ts
@../../../src/lib/mailer.ts
@../../../src/lib/email-template.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add password reset token persistence and helper module</name>
  <files>prisma/schema.prisma, src/lib/password-reset.ts</files>
  <action>
    Add a dedicated `PasswordResetToken` model with fields for user linkage, token, expiry, and created timestamp.
    Implement helper functions to:
    - generate secure random token
    - create/replace active token for a user with expiration (e.g., 1 hour)
    - validate token and resolve user
    - invalidate token after successful reset
    - send reset email via shared mailer + HTML template
    Reuse existing password hashing utility; do not duplicate hashing logic.
  </action>
  <verify>
    - `prisma/schema.prisma` contains `model PasswordResetToken`
    - `src/lib/password-reset.ts` exports token create/verify/invalidate + email send helpers
    - `npx prisma generate` succeeds
  </verify>
  <done>Password-reset domain helpers and persistence model are available for API routes</done>
</task>

<task type="auto">
  <name>Task 2: Implement forgot-password and reset-password API endpoints</name>
  <files>src/app/api/auth/forgot-password/route.ts, src/app/api/auth/reset-password/route.ts, src/templates/emails/reset-password.html</files>
  <action>
    Create `POST /api/auth/forgot-password` that accepts email and always returns generic success response to avoid account enumeration.
    If user exists, generate token and send HTML reset email with `resetUrl`.
    Create `POST /api/auth/reset-password` that accepts token, newPassword, and confirmPassword; enforce password strength and match checks.
    Validate token expiry, update user password hash, and invalidate token atomically.
    Add reset-password HTML template with CTA button and plain fallback support in sender logic.
  </action>
  <verify>
    - `POST /api/auth/forgot-password` returns 200 generic message for both known and unknown emails
    - `POST /api/auth/reset-password` rejects invalid/expired tokens and accepts valid token with strong password
    - Successful reset changes password hash and invalidates token
  </verify>
  <done>Backend password recovery flow is complete and wired to HTML reset email template</done>
</task>

</tasks>

<verification>
- Prisma schema and generated client include password reset token model
- Forgot-password endpoint prevents user enumeration
- Reset-password endpoint validates token + password strength + confirm match
- Token is one-time use and expires correctly
- Reset email uses HTML template file and shared mailer
</verification>

<success_criteria>
Phase 05-02 is complete when users can request password reset links, set a new password through a valid token flow, and all reset emails are template-based HTML.
</success_criteria>

<output>
After completion, create `.gsd/phases/05-adjust-send-email-smtp-env-configuration-add-forgot-password-and-verify-password-for-register-send-email-notification-after-supervisor-approval-and-add-function-to-read-html-format/05-02-SUMMARY.md`
</output>
