---
phase: 05-adjust-send-email-smtp-env-configuration-add-forgot-password-and-verify-password-for-register-send-email-notification-after-supervisor-approval-and-add-function-to-read-html-format
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mailer.ts
  - src/lib/email-template.ts
  - src/lib/email.ts
  - src/lib/notifications.ts
  - src/templates/emails/verification.html
  - src/templates/emails/approval.html
  - src/templates/emails/rejection.html
  - .env
autonomous: true

must_haves:
  truths:
    - 'All auth and supervision emails are sent using HTML template files'
    - 'SMTP configuration is read reliably from .env without key mismatch failures'
    - 'Users receive approval notification email after supervisor accepts profile'
  artifacts:
    - path: 'src/lib/email-template.ts'
      provides: 'Function that reads HTML email templates and injects variables'
      exports: ['renderEmailTemplate']
    - path: 'src/lib/mailer.ts'
      provides: 'Central SMTP client creation from environment variables'
      exports: ['createMailerClient']
    - path: 'src/templates/emails/approval.html'
      provides: 'HTML email template for supervisor approval notification'
      contains: '{{userName}}'
  key_links:
    - from: 'src/lib/email.ts'
      to: 'src/templates/emails/verification.html'
      via: 'renderEmailTemplate("verification", ... )'
      pattern: 'renderEmailTemplate\\(\\s*["\'']verification["\'']'
    - from: 'src/lib/notifications.ts'
      to: 'src/templates/emails/approval.html'
      via: 'renderEmailTemplate("approval", ... )'
      pattern: 'renderEmailTemplate\\(\\s*["\'']approval["\'']'
    - from: 'src/app/api/supervisor/approve/route.ts'
      to: 'src/lib/notifications.ts'
      via: 'sendApprovalNotification(user.email, ...) call'
      pattern: 'sendApprovalNotification\\('
---

<objective>
Standardize email delivery around SMTP environment configuration and file-based HTML templates, including supervisor approval notification emails.

Purpose: Remove fragile inline email markup and env-key mismatches so transactional emails are maintainable and reliable.
Output: Shared mailer + HTML template reader + template-driven verification/approval/rejection email sending.
</objective>

<execution_context>
@../../../.github/skills/execute-plan/SKILL.md
@../../../.gsd/templates/summary.md
</execution_context>

<context>
@../../../.gsd/PROJECT.md
@../../../.gsd/ROADMAP.md
@../../../.gsd/STATE.md
@../../../src/lib/email.ts
@../../../src/lib/notifications.ts
@../../../src/app/api/supervisor/approve/route.ts
@../../../.env
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement shared SMTP client and HTML template reader</name>
  <files>src/lib/mailer.ts, src/lib/email-template.ts, .env</files>
  <action>
    Create a single SMTP client factory that reads host/port/secure/user/pass/from from .env and returns a reusable transporter + from address.
    Support both password/from key variants to prevent breakage (`SMTP_PASSWORD` or `SMTP_PASS`, `SMTP_FROM` or legacy typo key) while standardizing docs and env to correct keys.
    Create a template reader that loads `src/templates/emails/*.html` and interpolates `{{variable}}` placeholders.
    Keep API small: one function to render a named template with variables.
  </action>
  <verify>
    - `src/lib/mailer.ts` exports `createMailerClient`
    - `src/lib/email-template.ts` exports `renderEmailTemplate`
    - `.env` has valid sender key (`SMTP_FROM`) and valid format with closing `>`
  </verify>
  <done>Mailer and HTML-template reader exist and are usable from auth/notification modules</done>
</task>

<task type="auto">
  <name>Task 2: Migrate verification and supervisor decision emails to HTML template files</name>
  <files>src/lib/email.ts, src/lib/notifications.ts, src/templates/emails/verification.html, src/templates/emails/approval.html, src/templates/emails/rejection.html</files>
  <action>
    Replace inline HTML strings with `renderEmailTemplate(...)` calls for verification, approval, and rejection emails.
    Keep plain-text fallback body in `text` for deliverability.
    Ensure approval notification is sent immediately after supervisor approval flow remains successful.
    Keep existing subjects and high-level copy intact; only move markup source to template files.
  </action>
  <verify>
    - `sendVerificationEmail` uses `renderEmailTemplate('verification', ...)`
    - `sendApprovalNotification` uses `renderEmailTemplate('approval', ...)`
    - `sendRejectionNotification` uses `renderEmailTemplate('rejection', ...)`
    - `src/app/api/supervisor/approve/route.ts` still calls `sendApprovalNotification(...)`
  </verify>
  <done>All current transactional emails are template-driven HTML and supervisor approval notification remains wired</done>
</task>

</tasks>

<verification>
- SMTP settings read from expected env keys without runtime key mismatch
- Email HTML comes from files in `src/templates/emails/`
- Approval/rejection/verification emails still send with text fallback
- Supervisor approval route keeps notification call intact
- `npm run build` passes for modified modules (ignoring unrelated pre-existing app prerender issues)
</verification>

<success_criteria>
Phase 05-01 is complete when SMTP config is centralized, HTML email content is loaded from template files, and supervisor approval notification email is sent through the new template-based pipeline.
</success_criteria>

<output>
After completion, create `.gsd/phases/05-adjust-send-email-smtp-env-configuration-add-forgot-password-and-verify-password-for-register-send-email-notification-after-supervisor-approval-and-add-function-to-read-html-format/05-01-SUMMARY.md`
</output>
