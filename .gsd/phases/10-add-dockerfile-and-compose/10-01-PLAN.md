---
phase: 10-add-dockerfile-and-compose
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - next.config.mjs
autonomous: true

must_haves:
  truths:
    - 'Repository can build a production Docker image for the Next.js app without manual file copying'
    - 'Container runtime includes only production-ready Next.js output and required runtime dependencies'
    - 'Container startup command is defined and compatible with Next.js server mode'
  artifacts:
    - path: 'Dockerfile'
      provides: 'Multi-stage production image build for Next.js + Prisma runtime'
      contains: 'deps/builder/runner stages with npm install, next build, and runtime CMD'
    - path: '.dockerignore'
      provides: 'Reduced build context for fast and deterministic Docker builds'
      contains: 'node_modules, .next, .gsd, git metadata, local env files'
    - path: 'next.config.mjs'
      provides: 'Standalone output mode for smaller production image'
      contains: "output: 'standalone'"
  key_links:
    - from: 'next.config.mjs'
      to: 'Dockerfile build stage'
      via: 'next build output expectations'
      pattern: "output:\s*'standalone'"
    - from: 'Dockerfile runner stage'
      to: 'Next.js runtime entrypoint'
      via: 'CMD'
      pattern: 'CMD'
---

<objective>
Create a production-ready Docker image definition for the existing Next.js + Prisma application.

Purpose: Standardize containerized deployment so Phase 10 can run consistently across machines and hosting targets.
Output: Multi-stage `Dockerfile` + `.dockerignore` + Next.js standalone configuration for efficient runtime image.
</objective>

<execution_context>
@../../../.github/skills/execute-plan/SKILL.md
@../../../.gsd/templates/summary.md
</execution_context>

<context>
@../../../.gsd/ROADMAP.md
@../../../.gsd/STATE.md
@../../../package.json
@../../../next.config.mjs
@../../../prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Next.js for standalone production container output</name>
  <files>next.config.mjs</files>
  <action>
    Update Next.js config to include `output: 'standalone'` while preserving existing image/optimization settings.
    Do not remove current production tuning fields (`compress`, `poweredByHeader`, `reactStrictMode`).
    This is required so Docker runtime stage can copy minimal standalone server output instead of full source tree.
  </action>
  <verify>Run `npm run build` and confirm `.next/standalone` is generated</verify>
  <done>Build output includes standalone server artifacts required by container runtime</done>
</task>

<task type="auto">
  <name>Task 2: Add multi-stage Dockerfile and build-context ignore rules</name>
  <files>Dockerfile, .dockerignore</files>
  <action>
    Create a multi-stage Node 20 Alpine Dockerfile (`deps`, `builder`, `runner`) for this Next.js app.
    In deps stage, install system compatibility package and project dependencies. Prefer lockfile-aware install (`npm ci` when lockfile exists, fallback to `npm install` when absent).
    In builder stage, copy source and run `npm run build`.
    In runner stage, copy standalone server output, static assets, and Prisma runtime artifacts, set `NODE_ENV=production`, expose port 3000, and define startup command with migration deploy before app start (`npx prisma migrate deploy && node server.js`).
    Add `.dockerignore` to exclude `node_modules`, `.next`, `.git`, `.gsd`, local env files, logs, and editor artifacts.
  </action>
  <verify>Run `docker build -t kenalyuk:phase10 .` successfully from repo root</verify>
  <done>Docker image builds successfully and contains runnable Next.js standalone server entrypoint</done>
</task>

</tasks>

<verification>
- `next build` produces standalone output for runtime copy
- Docker build succeeds from clean repo root
- Runtime stage exposes port 3000 and has valid start command
</verification>

<success_criteria>
10-01 is complete when a production-oriented Docker image can be built locally with no ad hoc steps and uses standalone output for minimal runtime payload.
</success_criteria>

<output>
After completion, create `.gsd/phases/10-add-dockerfile-and-compose/10-01-SUMMARY.md`
</output>
