---
phase: 10-add-dockerfile-and-compose
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - docker-compose.yml
  - .env.docker.example
  - package.json
autonomous: true

must_haves:
  truths:
    - 'A single docker compose command can start app + PostgreSQL together for local containerized execution'
    - 'App service receives all required environment variables without hardcoding secrets in compose file'
    - 'PostgreSQL data and uploads persist across compose restarts via named volumes'
  artifacts:
    - path: 'docker-compose.yml'
      provides: 'Two-service local stack (app + db) with health checks and dependency wiring'
      contains: 'services.app, services.db, volumes, depends_on with health condition'
    - path: '.env.docker.example'
      provides: 'Container-specific environment template for app/db wiring'
      contains: 'DATABASE_URL, AUTH_SECRET, NEXTAUTH_URL, SMTP_*, UPLOAD_DIRECTORY'
    - path: 'package.json'
      provides: 'Convenience scripts for compose lifecycle'
      contains: 'docker:up, docker:down, docker:logs'
  key_links:
    - from: 'docker-compose.yml app service'
      to: 'Dockerfile'
      via: 'build context and Dockerfile default path'
      pattern: 'build:'
    - from: '.env.docker.example'
      to: 'docker-compose.yml app environment'
      via: 'env_file and variable substitution'
      pattern: 'DATABASE_URL|AUTH_SECRET|NEXTAUTH_URL'
    - from: 'docker-compose.yml db service'
      to: 'app readiness'
      via: 'healthcheck + depends_on'
      pattern: 'healthcheck|depends_on'
---

<objective>
Compose the full local container stack (application + PostgreSQL) with reproducible environment wiring and operational scripts.

Purpose: Make Phase 10 usable end-to-end by enabling one-command startup for infrastructure and app runtime.
Output: `docker-compose.yml`, `.env.docker.example`, and npm scripts for routine compose operations.
</objective>

<execution_context>
@../../../.github/skills/execute-plan/SKILL.md
@../../../.gsd/templates/summary.md
</execution_context>

<context>
@../../../.gsd/ROADMAP.md
@../../../.gsd/STATE.md
@../../../package.json
@../../../prisma/schema.prisma
@../../../Dockerfile
@10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create compose stack for app and PostgreSQL with health-aware startup</name>
  <files>docker-compose.yml</files>
  <action>
    Create `docker-compose.yml` (Compose spec 3.9+) defining `db` (PostgreSQL 16 Alpine) and `app` (built from local Dockerfile).
    Configure named volume for PostgreSQL data persistence and named/bind volume for upload persistence mapped to app upload path.
    Add PostgreSQL healthcheck and gate app startup on healthy database state (`depends_on` with service health condition).
    Expose db and app ports for local use (`5432`, `3000`) and avoid embedding real secrets in file.
  </action>
  <verify>Run `docker compose config` and confirm configuration resolves without errors</verify>
  <done>Compose file defines valid app/db stack with health-gated dependency and persistent volumes</done>
</task>

<task type="auto">
  <name>Task 2: Add container environment template for app and database wiring</name>
  <files>.env.docker.example</files>
  <action>
    Create `.env.docker.example` with placeholders/defaults for compose execution.
    Include database credentials and app-level runtime variables used by current code: `DATABASE_URL`, `AUTH_SECRET`, `NEXTAUTH_URL`, `UPLOAD_DIRECTORY`, and SMTP settings.
    Ensure values are safe examples only and clearly structured so executor can copy into `.env.docker` (or `.env`) before running stack.
  </action>
  <verify>Confirm all referenced app env keys from `src/lib/*` used in container startup are represented in template</verify>
  <done>Environment template fully covers required runtime variables for docker-compose startup</done>
</task>

<task type="auto">
  <name>Task 3: Add npm helper scripts for compose lifecycle and smoke verification</name>
  <files>package.json</files>
  <action>
    Add scripts: `docker:up`, `docker:down`, and `docker:logs` using docker compose commands against `docker-compose.yml`.
    Keep existing scripts untouched and ensure JSON formatting remains valid.
    Smoke verify by running `npm run docker:up` (or `docker compose up -d --build`) and validate app endpoint responds on `http://localhost:3000`.
  </action>
  <verify>Run `npm run docker:up`, `docker compose ps`, and `curl -f http://localhost:3000` then `npm run docker:down`</verify>
  <done>Container stack can be started/stopped via npm scripts and app responds while db is healthy</done>
</task>

</tasks>

<verification>
- Compose specification validates and references built Docker image
- Env template covers app + db runtime requirements without hardcoded secrets
- App and db can run together with persistent volumes and deterministic startup order
</verification>

<success_criteria>
10-02 is complete when local containerized runtime is one-command operable (`docker:up`) and reliably starts the Next.js app against PostgreSQL.
</success_criteria>

<output>
After completion, create `.gsd/phases/10-add-dockerfile-and-compose/10-02-SUMMARY.md`
</output>
